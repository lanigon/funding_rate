# Database connection string used by the store crate (SeaORM)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/arbitrage

# Binance API credentials (optional, only required for private endpoints)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Engine/logging runtime settings
event_buffer_capacity=2048
RUST_LOG=info
